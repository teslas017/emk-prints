import{env}from"cloudflare:workers";import{requireOwnerApi}from"@/lib/admin-auth";import{cleanText}from"@/lib/validation";
export async function GET(){const denied=await requireOwnerApi();if(denied)return denied;const r=await env.DB.prepare("SELECT p.id,p.name,p.league,p.kit_type,p.price_kes,p.image_url,p.active,COALESCE(SUM(v.stock),0) total_stock,COALESCE(SUM(v.reserved),0) reserved FROM products p LEFT JOIN variants v ON v.product_id=p.id GROUP BY p.id ORDER BY p.created_at DESC").all();return Response.json({products:r.results})}
export async function DELETE(req:Request){const denied=await requireOwnerApi();if(denied)return denied;const id=cleanText(new URL(req.url).searchParams.get("id"),50);if(!id)return Response.json({error:"Product is required"},{status:400});const result=await env.DB.prepare("UPDATE products SET active=0 WHERE id=?").bind(id).run();return result.meta.changes?Response.json({removed:true}):Response.json({error:"Product not found"},{status:404})}
