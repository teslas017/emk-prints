import{env}from"cloudflare:workers";import{requireOwnerApi}from"@/lib/admin-auth";
export async function POST(req:Request){const denied=await requireOwnerApi();if(denied)return denied;const form=await req.formData(),file=form.get("file");if(!(file instanceof File)||file.size>5_000_000||!["image/jpeg","image/png","image/webp"].includes(file.type))return Response.json({error:"Use a JPG, PNG or WebP under 5MB"},{status:400});const key=`products/${crypto.randomUUID()}.${file.type.split("/")[1]}`;await env.BUCKET.put(key,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});return Response.json({url:`/api/images/${key}`},{status:201})}
