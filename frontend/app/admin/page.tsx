import{requireChatGPTUser}from"@/app/chatgpt-auth";import AdminClient from"./admin-client";export const dynamic="force-dynamic";export default async function Admin(){const user=await requireChatGPTUser("/admin");if(user.email.toLowerCase()!=="kiokojohn734@gmail.com")return <main style={{padding:40}}><h1>Owner access only</h1><p>This account is not authorised to manage EMK PRINTS.</p></main>;return <AdminClient/>}
